<h3 class="f-title">新建用户</h3>
<%= react_component('UserFormApp',
  props: {
    action: admin_users_path,
    method: 'POST',
    csrf_token: session[:_csrf_token],
    user: @user,
    roles: @roles,
    role_ids: @role_ids
  }
) %>
